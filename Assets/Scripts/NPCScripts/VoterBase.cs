using UnityEngine;

public class VoterBase : MonoBehaviour
{
    //選民基本邏輯
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
